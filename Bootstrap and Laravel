
*******in order to make bootstrap work go to terminal and run npm install********

<!doctype html>
<html lang="{{ app()->getLocale() }}">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="css/style.css" rel="stylesheet">
    <link href="css/app.css" rel="stylesheet">
</head>
<body>
//notice how this include is placed above the container so it will span the entire screen.
@include('include.nav')
//here we use a class container from bootstrap and use a partial/include. this means that all the code from the include will now be nested
//within the container class so the bootstrap here is affecting the code located in another file.On top of that @include('include.showcase')
//may contain some of its own bootstrap styling that will also need to be taken into consideration
<div class="container">
    @if(Request::is('contact'))
   @include('include.showcase')
    @endif
//within the row of bootstrap we can specify our columns and then yield the views and also use includes!
    <div class="row">
        <div class="col-md-8 col-lg-8">
            @yield('content')
        </div>
        <div class="col-md-4 col-lg-4">
            @include('include.sidebar')
        </div>
    </div>
</div>

<script src="js/script.js"></script>
</body>
</html>
