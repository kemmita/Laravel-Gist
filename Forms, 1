if directions below are weird go to https://laravelcollective.com/docs/master/html
run command composer require "laravelcollective/html":"^5.5.0"
Next, add your new provider to the providers array of config/app.php:
Collective\Html\HtmlServiceProvider::class,
then in the same file, find aliases and add   
'Form' => Collective\Html\FormFacade::class,
'Html' => Collective\Html\HtmlFacade::class,


contact blade
@extends('layout.mainlayout')

@section('content')
{{--Below we are using Laravel collective to create are form field. You can see the form open and close tags below--}}
        {!! Form::open(['url' => 'contact/submit']) !!}
{{--we use form-group classes to utuilze bootstrap form styling--}}
        <div class="form-group">
            {{--below first is the label, the first value is the name of the elemnt and the second is the value that will be
            displayed on the screen. Next we have the actual form area, labeled form-text so we know what type of data wil be
            entered here. the first value name is the name of the element. next we have an empty parameter as it appears in the
            actual input field and we do not want that, nex we declare our class and give it the bootstrap class of form-control--}}
            {{Form::label('name', 'Name')}}
            {{Form::text('name', '', ['class' => 'form-control', 'placeholder' => 'Enter Name'])}}
            </div>
 //below is a traditional way of using forms with bootstrap and you can mix.
              <div class="form-group">
                <label for="email">Email</label>
                <input name="email" class="form-control" placeholder="email">
                </div>
        <div class="form-group">
            {{Form::label('Message', 'Message')}}
            {{Form::textarea('Message','', ['class' => 'form-control', 'placeholder' => 'Jargon'])}}
        </div>
        <div>
            {{Form::submit('Submit', ['class' => 'btn btn-success'])}}
        </div>

        {!! Form::close() !!}

@endsection

//then go into web.php and create your route
//since we are grabbing a form we use the post method in place of get 
//we set the url and the call on a controller and an exact method from the chosen controller
Route::post('/contact/submit', 'MessagesController@submit');
